{
	"name": "@verifyistic/storage",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "StorageProvider abstraction — signing domain never couples to R2/S3 SDKs directly (doc 03 §8, doc 09).",
	"exports": {
		".": "./src/index.ts",
		"./local": "./src/local.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@verifyistic/core": "workspace:*"
	},
	"devDependencies": {
		"vitest": "^3.0.0"
	}
}
