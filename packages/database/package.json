{
	"name": "@verifyistic/database",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Kysely schema types + portable SQL migrations + runtime-agnostic migration runner. migrations/ is the single source of truth for schema.",
	"exports": {
		".": "./src/index.ts",
		"./d1": "./src/dialects/d1.ts",
		"./node": "./src/node.ts",
		"./testing": "./src/testing.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@verifyistic/core": "workspace:*",
		"kysely": "^0.28.0"
	},
	"devDependencies": {
		"vitest": "^3.0.0"
	}
}
