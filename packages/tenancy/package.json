{
	"name": "@verifyistic/tenancy",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "TenantContext + tenant-scoped repositories. Every query carries organization context — there is no repository API that queries by id alone (ADR-003).",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@verifyistic/core": "workspace:*",
		"@verifyistic/database": "workspace:*",
		"kysely": "^0.28.0"
	},
	"devDependencies": {
		"vitest": "^3.0.0"
	}
}
